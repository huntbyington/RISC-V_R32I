*.vcd
*_sim
*_sim.exe
out/*

# riscof-env test output -- fully regenerated by `make test` on every run
# (rm -rf'd and rebuilt each time), so tracking it is pure repo churn.
riscof-env/riscof_work/

# OS/editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
*~